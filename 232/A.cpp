#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
typedef long long ll;
using namespace std;

int main(){

    string s;
    cin >> s;
    int a,b;
    a = s[0] -'0';
    b = s[2] -'0';

    cout << a*b << endl;

    return 0;
}