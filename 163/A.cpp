#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define repi(i,a,b) for(int i = a; i <= (int)(b); i++)
typedef long long ll;
using namespace std;

int main(){

    ll r;
    cin >> r;

    double PI = acos(-1);

    cout << setprecision(20) << 2 * PI * r << endl;
    //cout << PI << endl;

    return 0;
}